apply plugin: "war"

bootWar {
    mainClass = "org.pickwicksoft.libraary.LibraaryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
